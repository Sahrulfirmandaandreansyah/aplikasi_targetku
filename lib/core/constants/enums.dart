// Enum untuk Status Target
enum TargetStatus {
  inProgress, // Sedang berjalan
  completed,  // Selesai
}

// Enum untuk Frekuensi Menabung
enum PlanFrequency {
  daily,   // Harian
  weekly,  // Mingguan
  monthly, // Bulanan
}

// Enum untuk Tipe Transaksi
enum TransactionType {
  increase, // Pemasukan
  decrease, // Pengeluaran
}